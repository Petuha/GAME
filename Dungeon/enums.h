#pragma once

enum class squaretype {
	floor, ladder, door, wall, LAST_ITEM
};

enum class itemtype {
	weapon, potion, armor, keys, artifactarmor, artifactweapon, LAST_ITEM
};

enum class species {
	human, skeleton, demon, LAST_ITEM
};

enum class atribute {
	force, agility, maxhp, LAST_ITEM
};

enum class direction {
	up, down, left, right, LAST_ITEM
};

enum class armortype {
	head, chest, legs, LAST_ITEM
};